import styled from "styled-components";

const Section = styled.section`
  position: relative;
  margin: 64px 0;
  overflow-x: auto;

  & > h2 {
    margin-bottom: 8px;
  }
`;

export default Section;
